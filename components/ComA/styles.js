import {StyleSheet} from 'react-native';

export default StyleSheet.create({
    container:{
        backgroundColor:"red",
        flex:1,
        alignContent:"center",
        alignItems:"center",
        marginTop:100
    }
});